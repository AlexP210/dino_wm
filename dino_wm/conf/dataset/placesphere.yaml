# PlaceSphere-v1 recordings (datasets/placesphere_dset.py). See pushcube.yaml on why these
# mirror the env configs' dataset blocks rather than referencing them.
_target_: "datasets.placesphere_dset.load_placesphere_slice_train_val"
n_rollout: null
normalize_action: ${normalize_action}
data_path: ${data_dir}/maniskill/PlaceSphere-v1.1-ppo-visual-wrist-224-pd_ee_delta_pos/demos/trajectory.dino16.mmap.frac1.0.h5
split_ratio: 0.8
transform:
  _target_: "datasets.img_transforms.default_transform"
  img_size: ${img_size}
