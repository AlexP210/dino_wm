# PickCube-v1 recordings (datasets/pickcube_dset.py). See pushcube.yaml on why these
# mirror the env configs' dataset blocks rather than referencing them.
_target_: "datasets.pickcube_dset.load_pickcube_slice_train_val"
n_rollout: null
normalize_action: ${normalize_action}
data_path: ${data_dir}/maniskill/PickCube-v1.1-ppo-staged-pd_ee_delta_pos/videos/trajectory.rgb.pd_ee_delta_pos.physx_cuda.dino16.mmap.frac1.0.h5
split_ratio: 0.8
transform:
  _target_: "datasets.img_transforms.default_transform"
  img_size: ${img_size}
